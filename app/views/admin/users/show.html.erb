<div>

</div>
<div class="row text-center white-text" id="add-monster-banner">
<h1><%= @user.username %></h1>
</div>
<div class="row text-center" id="add-background-light">
  <% if !@user.avatar.nil? %>
    <%= @user.avatar %>
  <% end %>
  <h2><%= @user.email %></h2>

  <%= link_to 'All Users', admin_users_path %>
   |
  <%= link_to 'Delete User', [:admin, @user], method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
