<%= form_for(@vote, url: handle_vote_monster_review_votes_path(@monster, review)) do |f| %>
  <%= hidden_field_tag(:value, '-') %>
  <%= f.submit 'Disagree', class: "button", id: "downvote", data: {monsterId: @monster.id, reviewId: review.id} %>
<% end %>
