<div class="row text-center white-text" id="add-monster-banner">
  <h1>Editing <%= @monster.name %></h1>
</div>
<div class="row text-center" id="add-background-light">
  <%= render 'form' %>
</div>

<br />
<%= link_to 'Delete', @monster, method: :delete, data: { confirm: 'Are you sure?' } %>
 |
 <%= link_to 'Back', @monster %>
